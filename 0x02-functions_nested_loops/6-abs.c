#include "main"

/**
 *_abs - computer ablsolute value of an integer
 *@i: The integer to be computed
 *Return: The absoluter value
 */
int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (-i);
}
